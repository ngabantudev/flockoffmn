# Public base URL of the Cloudflare R2 bucket hosting PMTiles archives
# (city wards, flock camera locations). Fetched client-side, so it must be
# prefixed PUBLIC_ to be exposed to the browser. Leave unset locally to
# render the basemap only, with data layers skipped.
PUBLIC_PMTILES_BASE_URL=
