---
import Base from '~/layouts/Base.astro';
import Data from '~/components/pages/Data.astro';
import { useTranslations } from '~/lib/i18n';

const locale = 'en' as const;
const t = useTranslations(locale);
---

<Base title={t('navData')}>
  <Data locale={locale} />
</Base>
