---
import Base from '~/layouts/Base.astro';
import News from '~/components/pages/News.astro';
import { useTranslations } from '~/lib/i18n';

const locale = 'es' as const;
const t = useTranslations(locale);
---

<Base title={t('newsTitle')}>
  <News locale={locale} />
</Base>
