---
import Base from '~/layouts/Base.astro';
import Act from '~/components/pages/Act.astro';
import { useTranslations } from '~/lib/i18n';

const locale = 'en' as const;
const t = useTranslations(locale);
---

<Base title={t('navAct')}>
  <Act locale={locale} />
</Base>
